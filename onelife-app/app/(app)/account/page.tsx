import { SectionHeader } from "@/components/SectionHeader";

export default function AccountPage() {
  return (
    <div className="space-y-10 pt-8">
      <SectionHeader
        eyebrow="Pass 5 · polish"
        title="Account arrives later."
        description="Orders, subscriptions, stores, profile. Deferred so Shop and Consult get the attention they deserve first."
      />
    </div>
  );
}
