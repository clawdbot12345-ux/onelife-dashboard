import { SectionHeader } from "@/components/SectionHeader";

export default function ConsultPage() {
  return (
    <div className="space-y-10 pt-8">
      <SectionHeader
        eyebrow="Pass 4 · in progress"
        title="Consult arrives next."
        description="Three booking modes, protocol assistant, and the pharmacist roster. For now, Home surfaces today's next two slots."
      />
    </div>
  );
}
