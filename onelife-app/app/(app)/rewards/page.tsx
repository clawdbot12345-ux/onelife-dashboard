import { SectionHeader } from "@/components/SectionHeader";

export default function RewardsPage() {
  return (
    <div className="space-y-10 pt-8">
      <SectionHeader
        eyebrow="Pass 4 · in progress"
        title="Rewards arrives next."
        description="Membership card, tier progress, earning ways, redemption catalogue. The card component is already live on /design."
      />
    </div>
  );
}
